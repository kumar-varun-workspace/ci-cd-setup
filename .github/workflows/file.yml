name: Basic Workflow
on:
  workflow_dispatch:
    inputs:
       groups: 
          description: "groups"
          required: true
          default: "smoke"
jobs:
  test:
    runs-on: [self-hosted]
    steps:
    - run: echo "Hello world!"
  
