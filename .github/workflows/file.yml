name: Basic Workflow
on:
  workflow_dispatch:
    inputs:
       groups: 
          description: "groups"
          required: true
          default: "smoke"
       xmlFiles:
          description: "xmlFiles"
          required: true
          default: "smokeGrid.xml"
jobs:
  test:
    runs-on: [self-hosted]
    steps:
    - run: echo "Hello world!"
  
